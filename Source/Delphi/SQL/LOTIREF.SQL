SET TRUENULLCREATE=OFF;
CREATE TABLE LotIRef in DICTIONARY USING 'LOT.FIL' (
Ingredient VARCHAR(8) NOT NULL CASE,
MachineID USMALLINT NOT NULL,
Lot_No VARCHAR(8) NOT NULL CASE
);
CREATE UNIQUE NOT MODIFIABLE INDEX "ByIngredientMID" ON LotIRef (Ingredient,MachineID);
SET TRUENULLCREATE=ON;
//NOTE: ONCE CREATED CHANGE DATA TYPE FOR VARCHAR FIELDS TO (10) LSTRING